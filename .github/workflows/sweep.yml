name: Issue Sweep

on:
  schedule:
    - cron: "0 10,22 * * *"
  workflow_dispatch:

permissions:
  issues: write

concurrency:
  group: daily-issue-sweep

jobs:
  sweep:
    runs-on: ubuntu-latest
    steps:
      - name: Enforce lifecycle timeouts
        uses: actions/github-script@v7
        with:
          script: |
            // All issue closing is done manually.
            // This workflow is retained for future automated label management.
            console.log('Sweep complete — no auto-close rules active.');
