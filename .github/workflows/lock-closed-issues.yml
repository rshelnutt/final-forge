name: Lock Stale Issues

on:
  schedule:
    - cron: "0 14 * * *"
  workflow_dispatch:

permissions:
  issues: write

concurrency:
  group: lock-threads

jobs:
  lock-closed-issues:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Note
        uses: actions/github-script@v7
        with:
          script: |
            // Issue locking is disabled — all issue management is manual.
            console.log('Lock workflow disabled.');
